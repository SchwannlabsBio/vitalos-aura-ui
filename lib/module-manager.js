import { ModuleStore } from "@/context/ModuleContext";

export default function ModuleManager(message) {
    const setModuleState = ModuleStore(state => state.setModuleState);

    switch(message) {

    }

}